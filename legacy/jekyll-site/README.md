# Archived Jekyll site

This folder preserves the previous Jekyll-based website source during the migration to a plain HTML/CSS site.

Nothing here is part of the active homepage anymore. It is kept so personal content, old layouts, publication pages, project pages, and build configuration are still available if needed later.
